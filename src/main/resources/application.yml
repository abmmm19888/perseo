spring:
  application:
    name: Perseo

  datasource:
    url: jdbc:mysql://localhost:3306/perseo
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080

# JWT Configuration (If needed)
jwt:
  secret: your_secret_key_here
